commands:
  install: pnpm install
  build: pnpm build
  test: npx vitest run --reporter=verbose
  typecheck: pnpm typecheck
  lint: pnpm lint
  seed: pnpm db:seed

services:
  web:
    start: cd /Users/wendy/wcstubhub-clone && PORT=3000 pnpm dev
    stop: lsof -ti :3000 | xargs kill 2>/dev/null || true
    healthcheck: curl -sf http://localhost:3000
    port: 3000
    depends_on: []
